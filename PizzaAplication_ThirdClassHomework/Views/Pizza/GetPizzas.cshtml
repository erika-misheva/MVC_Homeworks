@using PizzaAplication.Models.Domain;
@using PizzaAplication.Models.Mapper;
@using PizzaAplication.Models.ViewModels

@model List<PizzaViewModel>

<h2>Pizzas:</h2>

@*the content of _PizzaGreeting.cshtml will be replaced here*@
<partial name="~/Views/Shared/Partial/_PizzaGreeting.cshtml" />

@*Model is of type List<Pizza>*@
<ul>

    @foreach (var pizzaVM in @Model)
    {
        <partial name="~/Views/Shared/Partial/_PizzaDetailsPartial.cshtml" for="@pizzaVM" />
    }

    @*    @for (int i = 0; i < @Model.Count; i++)
    {
    var pizza = @Model[i];
    <partial name="~/Views/Shared/Partial/_PizzaDetailsPartial.cshtml" for="@pizza" />
    }*@
</ul>
